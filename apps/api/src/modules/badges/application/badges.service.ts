import { Injectable, Logger } from '@nestjs/common';

@Injectable()
export class BadgesService {
  private readonly logger = new Logger(BadgesService.name);

  // TODO: Implement badge business logic
}
