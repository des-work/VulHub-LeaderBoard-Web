{
  "name": "@vulhub/schema",
  "version": "1.0.0",
  "description": "Shared schemas and validation for VulHub Leaderboard",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@vulhub/config": "workspace:*",
    "typescript": "^5.2.2"
  }
}
